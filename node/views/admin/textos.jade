#wrapper  
  .content
    h1 Control de Textos

    .text_list.approved
      h2 Textos Aprobados
      ul
        each txt in textos
          -if(txt.checked) 
            li
              input.check_txt(type="checkbox", id="#{txt.id}", checked='checked')
              p #{txt.texto}

    .text_list.not-approved
      h2 Textos Sin Aprobar
      ul
        each txt in textos
          -if(!txt.checked) 
            li
              input.check_txt(type="checkbox", id="#{txt.id}")
              p #{txt.texto}

